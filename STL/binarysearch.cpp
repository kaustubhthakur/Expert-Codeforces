#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    vector<int>a(n);
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    int key ;
    cin>>key;
    //bool present = binary_search(a.begin(),a.end(),key);
   // auto present = lower_bound(a.begin(),a.end(),40);
  
    return 0;
}