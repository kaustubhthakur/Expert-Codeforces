#include <bits/stdc++.h>
using namespace std;
const int N = 10003;
int a[N];
int b[N];
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        for (int i = 1; i <= n; i++)
            cin >> a[i];
        for (int i = 1; i <= n; i++)
            cin >> b[i];
    
    }
    return 0;
}