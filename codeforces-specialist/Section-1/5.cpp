#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int N;
        std::cin >> N;
        for (int i = 1; i <= N; i++)
            cout << 2 * i << '\n';
    }
}