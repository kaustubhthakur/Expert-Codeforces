#include <bits/stdc++.h>
using namespace std;
void solve()
{
    string str;
  for(int i=1;i<=str.size();i++)
  {
    cin>>str[i];
  }
    set<char>st;
  for(int i=1;i<=str.size();i++)
  {
    for(int j=str.size();j>=1;j--)
    {
if(str[i]==str[j])
{
    
}
    }
  }
}
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        solve();
    }
    return 0;
}